# StakeNote Implementation

This is a Rust proof-of-concept implementation of StakeNote.

To run a single instance of the StakeNote block production run:
```
cargo run --release --bin run
```

To measure the block production and verification time run:
```
cargo run --release --bin measure
```
